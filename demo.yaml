---
name: ExampleOs
parameters:
  target: rootfs.tar.gz
  iamge: demo.img
steps:
  - step: bootstrap
    options:
      suite: bookworm
      target: rootfs
  - step: disk
    options:
      definitions: definitions
      size: 10G
  - step: deploy
    options:
      source: rootfs.tar.gz
      target: demo.img
  - step: shell
    options:
      chroot: True
      shell: |
        apt-get update -y
        apt-get install podman
